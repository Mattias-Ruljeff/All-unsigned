import React from 'react';

const Loading = () => {

    // The HTML that is being rendered
    return (
        <div className="loading">
            <h1>Loading...</h1>
        </div>
    );
}

// Exports
export default Loading;